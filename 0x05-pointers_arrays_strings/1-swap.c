#include "main.h"
#include <stdio.h>

/**
*void swap_int(int *a, int *b) - swaps the value of two intergers a and b
*@a: first integer
*@b: second integer
* Return : Always 0.
*/
void swap_int(int *a, int *b);
{
	int a;
	int b;
	int tmp = a;

	*a = b;
	*b = tmp;
}
