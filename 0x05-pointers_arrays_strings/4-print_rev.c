#include "main.h"

/** 
* print_rev - print a string in reverse
* @s : the string to be printed in rev
*/
void print_rev(char *s)
{
	int  i, n;

	
	for (i = n - 1; i >= 0; i--)
	{
		_putcha r(s[i]);
	}
}
